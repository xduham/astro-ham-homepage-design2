---
const { title, subtitle } = Astro.props;
---
<div class="flex flex-col items-center">
    <p class="text-4xl md:text-5xl font-semibold">{title}</p>
    <p class="font-light w-80 md:w-[32rem] text-center mt-2">
        {subtitle}
    </p>
</div>