---
const { title, subtitle } = Astro.props;
---
<div class="flex flex-col items-center">
    <p class="text-5xl font-semibold">{title}</p>
    <p class="font-extralight w-[32rem] text-center mt-2">
        {subtitle}
    </p>
</div>